/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["lh3.googleusercontent.com", "avatars.githubusercontent.com", "res.cloudinary.com", "plus.unsplash.com"], // Add the domain hosting your images
    },

};

export default nextConfig;
